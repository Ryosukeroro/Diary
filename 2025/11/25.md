単位行列のプログラム
```cpp
void dgc_transform_identity(dgc_transform_t t)
{
    int r, c;

    for(r = 0; r < 4; r++)
     for(c = 0; c < 4; c++)
      if(r==c)
        t[r][c] = 1;
      else
        t[r][c] = 0;
}
```
I'll text you when I get home.

Toeic単語100残り
